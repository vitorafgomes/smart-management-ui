apiVersion: apps/v1
kind: Deployment
metadata:
  name: mfe-users
  namespace: smart-management-ui
  labels:
    app: smart-management-ui
    component: mfe-users
spec:
  replicas: 2
  selector:
    matchLabels:
      app: smart-management-ui
      component: mfe-users
  template:
    metadata:
      labels:
        app: smart-management-ui
        component: mfe-users
    spec:
      containers:
        - name: mfe-users
          image: ${REGISTRY}/smart-management-ui/mfe-users:${TAG:-latest}
          imagePullPolicy: Always
          ports:
            - containerPort: 80
              protocol: TCP
          resources:
            requests:
              memory: "32Mi"
              cpu: "25m"
            limits:
              memory: "64Mi"
              cpu: "50m"
          livenessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          securityContext:
            runAsNonRoot: false
            readOnlyRootFilesystem: false
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: mfe-users
  namespace: smart-management-ui
  labels:
    app: smart-management-ui
    component: mfe-users
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: smart-management-ui
    component: mfe-users
