export const APP_NAME = 'Smart Management';

/**
 * The company wordmark the public surfaces carry, split the way the logo renders it: the second
 * half is set in a lighter weight. Inside the app the same logo shows the tenant instead.
 */
export const COMPANY_BRAND_MAIN = 'Chiacho';
export const COMPANY_BRAND_SECONDARY = ' & Gomes';
